% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transaction_ops.R
\name{add_complement}
\alias{add_complement}
\title{Add Complement Items}
\usage{
add_complement(x, items = NULL, prefix = "!")
}
\arguments{
\item{x}{A \code{dyna_transactions} object, or anything \code{\link[=transactions]{transactions()}}
accepts.}

\item{items}{Character vector of items to complement. Defaults to all
items.}

\item{prefix}{Prefix for the complement labels. Default \code{"!"}.}
}
\value{
A \code{dyna_transactions} object with the complement items added.
Sequences are dropped, because the absence of an item has no position
in a sequence.
}
\description{
Adds a negated item for each named item, so rules can be mined about
the \emph{absence} of a behaviour as well as its presence.
}
\examples{
tr <- transactions(list(c("a", "b"), c("a"), c("b")))
add_complement(tr, items = "a")
}
\seealso{
\code{\link[=transactions]{transactions()}}
}
