% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transaction_ops.R
\name{aggregate_items}
\alias{aggregate_items}
\title{Roll Items Up a Hierarchy}
\usage{
aggregate_items(x, hierarchy)
}
\arguments{
\item{x}{A \code{dyna_transactions} object, or anything \code{\link[=transactions]{transactions()}}
accepts.}

\item{hierarchy}{A named character vector mapping item to group, or a
two-column \code{data.frame} (\code{item}, \code{group}). Items absent from the map
keep their own label.}
}
\value{
A \code{dyna_transactions} object at the group level. Sequence order
is preserved; runs of the same group collapse to a single event.
}
\description{
Replaces items by the group they belong to, so mining runs at a coarser
level of description (individual actions to action categories, say).
}
\examples{
tr <- transactions(list(c("read", "quiz"), c("video", "quiz")))
aggregate_items(tr, c(read = "study", video = "study", quiz = "assess"))
}
\seealso{
\code{\link[=transactions]{transactions()}}
}
