% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network.R
\name{as_network}
\alias{as_network}
\title{Convert Mined Rules to a Plottable Network}
\usage{
as_network(
  x,
  weight = c("lift", "confidence", "support"),
  aggregate = c("max", "mean", "sum"),
  top = NULL
)
}
\arguments{
\item{x}{A \code{dynarules} object.}

\item{weight}{Character. Which rule metric becomes the edge weight:
\code{"lift"} (default), \code{"confidence"}, or \code{"support"}.}

\item{aggregate}{Character. How parallel edges combine: \code{"max"}
(default), \code{"mean"}, or \code{"sum"}.}

\item{top}{Integer or NULL. Use only the top N rules by lift.}
}
\value{
An object of class \code{c("dynarules_net", "cograph_network")} with
\verb{$weights} (item x item matrix), \verb{$nodes}, \verb{$edges} (integer
from/to + weight), \verb{$directed = TRUE}, \verb{$method}, \verb{$n}.
}
\description{
Builds an item-level directed network from a rule set: nodes are items,
and each rule contributes edges from every antecedent item to every
consequent item. Parallel edges (the same item pair reached by several
rules) are aggregated. The result mirrors the Nestimate / cograph
\code{netobject} layout and carries the \code{cograph_network} class, so it plots
directly with \code{cograph::splot(net)}.
}
\examples{
trans <- list(c("plan", "discuss", "reflect"),
              c("plan", "discuss", "execute"),
              c("plan", "reflect"),
              c("discuss", "reflect"))
fit <- dynarules(trans, min_support = 0.25, min_lift = 0)
net <- as_network(fit)
net$weights

}
