% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference.R
\name{bootstrap_rules}
\alias{bootstrap_rules}
\title{Bootstrap Stability of Mined Rules}
\usage{
bootstrap_rules(x, iter = 200L, conf = 0.95, seed = NULL)
}
\arguments{
\item{x}{A \code{dynarules} object.}

\item{iter}{Integer. Number of bootstrap resamples. Default 200.}

\item{conf}{Numeric. CI coverage. Default 0.95.}

\item{seed}{Integer or NULL. Seed for reproducibility.}
}
\value{
An object of class \code{"dynarules_boot"} with \verb{$stability}, a tidy
data.frame (one row per original rule): antecedent, consequent,
recovery, and mean / lower / upper for support, confidence, and lift.
}
\description{
Assesses how reproducible each mined rule is under resampling.
Transactions are resampled with replacement \code{iter} times; each resample
is re-mined with the original thresholds. A rule's \code{recovery} is the
fraction of resamples in which it is rediscovered; its metric intervals
are percentile bootstrap CIs over the resamples that recovered it.
}
\examples{
trans <- list(c("plan", "discuss", "reflect"),
              c("plan", "discuss", "execute"),
              c("plan", "reflect"),
              c("discuss", "reflect"),
              c("plan", "discuss", "reflect"))
fit <- dynarules(trans, min_support = 0.3, min_lift = 0)
bs <- bootstrap_rules(fit, iter = 50, seed = 1)
bs

}
\references{
Epskamp, S., Borsboom, D., & Fried, E. I. (2018). Estimating
psychological networks and their accuracy. \emph{Behavior Research
Methods}, 50, 195--212.
}
