% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cba.R
\name{cba}
\alias{cba}
\title{Build a Classifier From Association Rules}
\usage{
cba(x, class, min_support = 0.05, min_confidence = 0.5, max_length = 5L, ...)
}
\arguments{
\item{x}{A \code{dyna_transactions} object, or anything \code{\link[=transactions]{transactions()}}
accepts.}

\item{class}{Character vector naming the items that are class labels,
e.g. \code{c("pass", "fail")}. Every transaction must contain exactly one
of them.}

\item{min_support, min_confidence, max_length}{Passed to \code{\link[=dynarules]{dynarules()}}
for the rule-mining step.}

\item{...}{Further arguments for \code{\link[=dynarules]{dynarules()}}.}
}
\value{
An object of class \code{dynarules_cba}: the ordered \code{rules}, the
\code{default} class, and the training \code{accuracy}.
}
\description{
Mines rules whose consequent is a class label, then reduces them to an
ordered decision list with the CBA M1 algorithm.
}
\examples{
tx <- list(c("hot", "humid", "no"), c("hot", "windy", "no"),
           c("mild", "humid", "yes"), c("cool", "windy", "yes"),
           c("cool", "humid", "yes"), c("mild", "windy", "no"))
fit <- cba(tx, class = c("yes", "no"), min_support = 0.2,
           min_confidence = 0.5)
fit
predict(fit, tx)
}
\references{
Liu, B., Hsu, W. and Ma, Y. (1998). Integrating
classification and association rule mining. \emph{KDD-98}, 80--86.
}
\seealso{
\code{\link[=dynarules]{dynarules()}}, \code{\link[=rules]{rules()}}
}
