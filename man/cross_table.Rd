% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transaction_ops.R
\name{cross_table}
\alias{cross_table}
\title{Pairwise Item Co-Occurrence}
\usage{
cross_table(x, measure = c("count", "support", "lift"), diagonal = FALSE)
}
\arguments{
\item{x}{A \code{dyna_transactions} object, or anything \code{\link[=transactions]{transactions()}}
accepts.}

\item{measure}{\code{"count"} (default) for the number of transactions
holding both items, \code{"support"} for that as a fraction, or \code{"lift"}
for the ratio of joint to expected support.}

\item{diagonal}{Logical; keep the item-with-itself rows. Default
\code{FALSE}. For \code{measure = "lift"} those rows are \code{NA}: an item's lift
against itself is not defined.}
}
\value{
A tidy \code{data.frame}: \code{item1}, \code{item2}, \code{value}.
}
\description{
The cross table of every item pair, in tidy long form rather than as a
matrix.
}
\examples{
tr <- transactions(list(c("a", "b"), c("a", "c"), c("a", "b", "c")))
cross_table(tr)
cross_table(tr, measure = "lift")
}
\seealso{
\code{\link[=item_frequency]{item_frequency()}}, \code{\link[=affinity]{affinity()}}
}
