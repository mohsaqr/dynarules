% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discretize.R
\name{discretize_items}
\alias{discretize_items}
\title{Discretize a Numeric Variable Into Mining Items}
\usage{
discretize_items(
  x,
  method = c("frequency", "interval", "cluster", "fixed"),
  breaks = 3,
  labels = NULL,
  include_lowest = TRUE,
  right = FALSE,
  ordered = FALSE,
  infinity = FALSE,
  ...
)
}
\arguments{
\item{x}{A numeric vector, or a \code{data.frame} whose numeric columns are
all to be discretized.}

\item{method}{How to place the cut points:
\itemize{
\item \code{"frequency"} (default) — equal-frequency bins (quantiles), so each
bin holds roughly the same number of observations.
\item \code{"interval"} — equal-width bins across the observed range.
\item \code{"cluster"} — k-means on the values, cutting midway between
adjacent cluster centres; bins follow where the data actually
clumps.
\item \code{"fixed"} — cut points you supply yourself in \code{breaks}.
}}

\item{breaks}{Number of bins, or for \code{method = "fixed"} the vector of
cut points. Default \code{3}.}

\item{labels}{Optional character vector of bin labels.}

\item{include_lowest}{Include the lowest value in the first bin.
Default \code{TRUE}.}

\item{right}{Are intervals closed on the right? Default \code{FALSE}, i.e.
\verb{[a, b)}, matching the convention used for rule mining.}

\item{ordered}{Return an ordered factor? Default \code{FALSE}.}

\item{infinity}{Extend the outer breaks to \code{-Inf} and \code{Inf}, so values
outside the training range still fall in a bin. Default \code{FALSE}.}

\item{...}{Passed to \code{\link[base:cut]{base::cut()}}.}
}
\value{
A factor of the same length as \code{x}, or for a \code{data.frame} a
\code{data.frame} with its numeric columns replaced by factors.
}
\description{
Discretize a Numeric Variable Into Mining Items
}
\examples{
discretize_items(c(1, 3, 5, 7, 9, 11), breaks = 3)
discretize_items(c(1, 3, 5, 7, 9, 11), method = "interval", breaks = 2)
discretize_items(c(1, 2, 3, 10, 11, 12), method = "cluster", breaks = 2)
}
\seealso{
\code{\link[=transactions]{transactions()}}
}
