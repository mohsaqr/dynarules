% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mine.R
\name{dynarules}
\alias{dynarules}
\title{Mine Association or Sequential Rules}
\usage{
dynarules(
  x,
  type = c("cooccurrence", "sequential"),
  min_support = 0.1,
  min_confidence = 0.5,
  min_lift = 1,
  min_length = 2L,
  max_length = 5L,
  appearance = NULL,
  actor = NULL,
  action = NULL,
  time = NULL,
  session = NULL,
  unit = c("session", "actor", "window"),
  window = NULL,
  step = NULL,
  weights = NULL,
  gap = NULL,
  min_gap = NULL,
  window_size = NULL
)
}
\arguments{
\item{x}{A \code{dyna_transactions} object, or raw data accepted by
\code{\link[=transactions]{transactions()}} (long event log, wide data.frame, list, binary
matrix).}

\item{type}{Character. \code{"cooccurrence"} (default) or \code{"sequential"}.}

\item{min_support}{Numeric, above 0 and at most 1. Minimum fraction of
transactions containing the itemset/pattern. Default 0.1.}

\item{min_confidence}{Numeric between 0 and 1. Default 0.5.}

\item{min_lift}{Numeric >= 0. Default 1.}

\item{min_length}{Integer. Minimum number of items in a rule
(antecedent plus consequent). Default \code{2}.}

\item{max_length}{Integer >= 2. Maximum itemset/pattern size. Default 5.}

\item{appearance}{Optional list restricting where items may occur:
\code{lhs} and \code{rhs} whitelist the items allowed on each side, and \code{none}
drops items from the data before mining. \code{NULL} (default) places no
restriction.}

\item{actor, action, time, session, unit, window, step}{Event-log grammar,
forwarded to \code{\link[=transactions]{transactions()}} when \code{x} is raw data.}

\item{weights}{Optional numeric vector of transaction weights, one per
transaction. Support becomes weighted support: the share of total
weight rather than the share of transactions. \code{NULL} (default)
weights every transaction equally.}

\item{gap}{Maximum number of events allowed between consecutive items
of a sequential pattern. \code{NULL} (default) places no limit, i.e.
ordinary non-contiguous containment; \code{gap = 1} requires the items to
be adjacent. Sequential mining only.}

\item{min_gap}{Minimum number of events between consecutive items.
Sequential mining only.}

\item{window_size}{Maximum span, in events, from the first item of a
pattern to its last. Sequential mining only. (Distinct from
\code{window}, which cuts the transactions themselves.)}
}
\value{
An object of class \code{"dynarules"}:
\describe{
\item{rules}{Tidy data.frame, one row per rule: antecedent,
consequent, support, confidence, lift, conviction, count,
n_transactions. Sequential antecedents/consequents use
\code{" -> "} between ordered items; co-occurrence uses \code{", "}.}
\item{frequent}{Tidy data.frame of frequent itemsets/patterns.}
\item{type, items, n_transactions, params}{Metadata.}
\item{transactions}{The \code{dyna_transactions} object mined, kept so
\code{\link[=bootstrap_rules]{bootstrap_rules()}} and \code{\link[=permute_rules]{permute_rules()}} can re-mine.}
}
}
\description{
The single entry point for rule mining. \code{type = "cooccurrence"} mines
classic association rules (level-wise Apriori with full candidate
pruning): order within a transaction is ignored. \code{type = "sequential"}
mines order-aware rules (generalized sequential patterns): a rule
\verb{plan -> discuss => reflect} states that transactions containing
\code{plan} followed (not necessarily immediately) by \code{discuss} tend to
contain \code{reflect} after both.

Accepts a ready \code{transactions()} object, or raw data plus the event-log
grammar arguments, which are forwarded to \code{\link[=transactions]{transactions()}}.
}
\details{
Support of a sequential pattern is the fraction of transactions whose
ordered sequence contains the pattern as a (not necessarily contiguous)
subsequence. Both miners use downward closure: every sub-itemset /
subsequence of a frequent set is itself frequent, which the candidate
pruning step exploits.
}
\examples{
log <- data.frame(
  student = rep(c("s1", "s2", "s3", "s4"), each = 3),
  code = c("plan", "discuss", "reflect",
           "plan", "discuss", "execute",
           "plan", "reflect", "discuss",
           "discuss", "plan", "reflect"),
  stringsAsFactors = FALSE
)
fit <- dynarules(log, actor = "student", action = "code",
                 type = "sequential", min_support = 0.25)
fit

}
\seealso{
\code{\link[=transactions]{transactions()}}, \code{\link[=rules]{rules()}}, \code{\link[=as_network]{as_network()}},
\code{\link[=bootstrap_rules]{bootstrap_rules()}}, \code{\link[=permute_rules]{permute_rules()}}
}
