% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/algebra.R
\name{is_significant}
\alias{is_significant}
\title{Which Rules Beat Independence?}
\usage{
is_significant(x, alpha = 0.05, adjust = "BH")
}
\arguments{
\item{x}{A \code{dynarules} object.}

\item{alpha}{Significance level. Default \code{0.05}.}

\item{adjust}{Multiplicity correction, any method accepted by
\code{\link[stats:p.adjust]{stats::p.adjust()}}. Default \code{"BH"}; use \code{"none"} to switch it off.}
}
\value{
A logical vector with one entry per mined rule.
}
\description{
Tests each rule against the null that antecedent and consequent are
independent, using a one-sided Fisher's exact test on the rule's 2x2
contingency table, with multiplicity control across the rule set.
}
\details{
This is a test against \emph{chance co-occurrence given the item margins}.
It is not the same question as \code{\link[=permute_rules]{permute_rules()}}, which asks whether a
rule survives destroying the structure of the transactions themselves;
use that one when the transaction structure is what you are arguing
about.
}
\examples{
fit <- dynarules(list(c("a", "b", "c"), c("a", "b"), c("a", "c")),
                 min_support = 0.3, min_confidence = 0.3)
is_significant(fit)
}
\seealso{
\code{\link[=rules]{rules()}}, \code{\link[=permute_rules]{permute_rules()}}, \code{\link[=measures]{measures()}}
}
