% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/itemsets.R
\name{itemsets}
\alias{itemsets}
\title{Frequent Itemsets and Their Closed, Maximal and Generator Subsets}
\usage{
itemsets(
  x,
  target = c("frequent", "closed", "maximal", "generator"),
  min_size = NULL,
  max_size = NULL,
  top = NULL
)
}
\arguments{
\item{x}{A \code{dynarules} object.}

\item{target}{One of \code{"frequent"}, \code{"closed"}, \code{"maximal"},
\code{"generator"}.}

\item{min_size, max_size}{Optional integer bounds on the number of items
in a pattern.}

\item{top}{Optional integer; keep only the \code{top} patterns by support.}
}
\value{
A tidy \code{data.frame} with one row per pattern: \code{pattern},
\code{size}, \code{support}, \code{count}.
}
\description{
Returns the frequent itemsets (co-occurrence) or frequent sequential
patterns behind a mined rule set, optionally restricted to one of the
standard condensed representations.
}
\details{
\itemize{
\item \code{"frequent"} — every pattern meeting \code{min_support}.
\item \code{"closed"} — no proper superpattern has the same support. The
loss-less summary: supports of all frequent patterns are recoverable.
\item \code{"maximal"} — no proper superpattern is frequent at all. The
smallest summary, but supports of sub-patterns are lost.
\item \code{"generator"} — no proper subpattern has the same support; the
minimal patterns of each equivalence class.
}
}
\examples{
fit <- dynarules(list(c("a", "b", "c"), c("a", "b"), c("a", "c")),
                 min_support = 0.3, min_confidence = 0.3)
itemsets(fit)
itemsets(fit, target = "closed")
itemsets(fit, target = "maximal")
}
\seealso{
\code{\link[=rules]{rules()}}, \code{\link[=measures]{measures()}}
}
