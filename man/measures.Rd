% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/measures.R
\name{measures}
\alias{measures}
\title{Interest Measures for Mined Rules}
\usage{
measures(x, measure = NULL, counts = FALSE)
}
\arguments{
\item{x}{A \code{dynarules} object.}

\item{measure}{Character vector of measure names, or \code{NULL} (the
default) for the four headline measures: support, confidence, lift
and count. Use \code{"all"} for the entire catalogue.}

\item{counts}{Logical; when \code{TRUE}, also return the four contingency
counts (\code{n11}, \code{n10}, \code{n01}, \code{n00}) — the equivalent of the \code{table}
measure in other implementations. Defaults to \code{FALSE}.}
}
\value{
A tidy \code{data.frame} with one row per rule: \code{antecedent},
\code{consequent}, then one column per requested measure.
}
\description{
Computes any of the 51 association-rule interest measures for a mined
rule set. Call \code{\link[=list_measures]{list_measures()}} for the catalogue.
}
\details{
All measures are functions of the number of transactions and the three
supports P(A), P(B) and P(A and B), which together fix the rule's 2x2
contingency table. \code{improvement}, \code{boost}, \code{LIC} and \code{importance}
additionally compare each rule against the most favourable more
general rule (same consequent, a proper sub-antecedent); they are \code{NA}
-free by falling back to the independence baseline when the antecedent
is a single item.

For sequential rules the 2x2 table is an approximation: the support of
\code{A -> B} counts only occurrences in the right order, so it can fall
below the co-occurrence of \code{A} and \code{B} and leave the implied table
inconsistent. When that happens \code{measures()} warns once and the
affected contingency measures return \code{NA}; support, confidence and
lift are unaffected.
}
\examples{
fit <- dynarules(list(c("a", "b", "c"), c("a", "b"), c("a", "c")),
                 min_support = 0.3, min_confidence = 0.3)
measures(fit)
measures(fit, measure = c("kappa", "phi", "jaccard"))
}
\seealso{
\code{\link[=list_measures]{list_measures()}}, \code{\link[=rules]{rules()}}
}
