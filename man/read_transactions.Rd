% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transaction_ops.R
\name{read_transactions}
\alias{read_transactions}
\title{Read Transactions From a File}
\usage{
read_transactions(
  file,
  format = c("basket", "single"),
  sep = ",",
  header = FALSE,
  cols = c(1, 2)
)
}
\arguments{
\item{file}{Path to a text file.}

\item{format}{\code{"basket"} (default): one transaction per line, items
separated by \code{sep}. \code{"single"}: one item per line, with columns
identifying the transaction and the item.}

\item{sep}{Field separator. Default \code{","}.}

\item{header}{Does the file have a header line? Default \code{FALSE}.}

\item{cols}{For \code{format = "single"}, the transaction-id and item column
positions or names. Default \code{c(1, 2)}.}
}
\value{
A \code{dyna_transactions} object.
}
\description{
Read Transactions From a File
}
\examples{
f <- tempfile()
writeLines(c("a,b,c", "a,b", "b,c"), f)
read_transactions(f)
unlink(f)
}
\seealso{
\code{\link[=transactions]{transactions()}}
}
