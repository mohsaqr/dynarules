% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network_rule.R
\name{rule_key}
\alias{rule_key}
\title{Draw the Size and Colour Key for a Rule Network}
\usage{
rule_key(
  net,
  position = c("bottomleft", "bottomright", "topleft", "topright"),
  cex = 0.75
)
}
\arguments{
\item{net}{A rule-level network from \code{\link[=as_network]{as_network()}} with
\code{level = "rule"}.}

\item{position}{Corner for the key: \code{"bottomleft"} (default),
\code{"bottomright"}, \code{"topleft"}, \code{"topright"}.}

\item{cex}{Text size. Default \code{0.75}.}
}
\value{
\code{net}, invisibly.
}
\description{
cograph's legend covers discrete groups, edge colours and a node-size
scale, but not the two continuous encodings an association-rule graph
uses. This draws that key onto the current plot after
\code{\link[cograph:splot]{cograph::splot()}} has run.
}
\examples{
fit <- dynarules(list(c("a", "b", "c"), c("a", "b"), c("a", "c")),
                 min_support = 0.3, min_confidence = 0.3)
net <- as_network(fit, level = "rule")
if (requireNamespace("cograph", quietly = TRUE)) {
  cograph::splot(net)
  rule_key(net)
}
}
\seealso{
\code{\link[=as_network]{as_network()}}
}
