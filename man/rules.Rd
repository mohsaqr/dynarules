% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rules.R
\name{rules}
\alias{rules}
\title{Extract, Filter, and Rank Mined Rules}
\usage{
rules(
  x,
  min_support = 0,
  min_confidence = 0,
  min_lift = 0,
  items = NULL,
  side = c("any", "antecedent", "consequent"),
  redundant = TRUE,
  significant = FALSE,
  maximal = FALSE,
  alpha = 0.05,
  adjust = "BH",
  top = NULL,
  by = c("lift", "confidence", "support")
)
}
\arguments{
\item{x}{A \code{dynarules} object.}

\item{min_support, min_confidence, min_lift}{Numeric. Tighten the
corresponding threshold post hoc (defaults keep everything mined).}

\item{items}{Character or NULL. Keep only rules mentioning at least one
of these items on the chosen \code{side}.}

\item{side}{Character. Where \code{items} must appear: \code{"any"} (default),
\code{"antecedent"}, or \code{"consequent"}.}

\item{redundant}{Logical. \code{TRUE} (default) keeps all rules. \code{FALSE}
drops rules dominated by a more general rule: same consequent, the
general antecedent contained in the specific one, and confidence at
least as high.}

\item{significant}{Keep only rules that beat independence; see
\code{\link[=is_significant]{is_significant()}}. Default \code{FALSE}.}

\item{maximal}{Keep only rules built on a maximal pattern; see
\code{\link[=is_maximal]{is_maximal()}}. Default \code{FALSE}.}

\item{alpha, adjust}{Passed to \code{\link[=is_significant]{is_significant()}} when
\code{significant = TRUE}.}

\item{top}{Integer or NULL. Keep the top N rules after sorting.}

\item{by}{Character. Sort key: \code{"lift"} (default), \code{"confidence"}, or
\code{"support"}. Descending, confidence as tie-break.}
}
\value{
A tidy data.frame, one row per rule (same columns as
\code{x$rules}).
}
\description{
The tidy accessor for a mined rule set. All narrowing and ordering is
done through arguments; the returned data.frame is printed directly.
}
\examples{
trans <- list(c("plan", "discuss", "reflect"),
              c("plan", "discuss", "execute"),
              c("plan", "reflect"),
              c("discuss", "reflect"))
fit <- dynarules(trans, min_support = 0.25, min_lift = 0)
rules(fit, min_lift = 1, top = 5)

}
