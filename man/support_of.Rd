% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transaction_ops.R
\name{support_of}
\alias{support_of}
\title{Support of Arbitrary Itemsets}
\usage{
support_of(x, itemsets, type = c("cooccurrence", "sequential"))
}
\arguments{
\item{x}{A \code{dyna_transactions} object, or anything \code{\link[=transactions]{transactions()}}
accepts.}

\item{itemsets}{A character vector (one itemset) or a list of character
vectors.}

\item{type}{\code{"cooccurrence"} (default) counts transactions containing
all the items in any order; \code{"sequential"} requires them in the given
order.}
}
\value{
A tidy \code{data.frame}: \code{pattern}, \code{size}, \code{support}, \code{count}.
}
\description{
Support of itemsets you name yourself, without mining. Useful for
checking a hypothesis against the data directly.
}
\examples{
tr <- transactions(list(c("a", "b"), c("a", "c"), c("a", "b", "c")))
support_of(tr, list(c("a", "b"), c("b", "c"), "a"))
}
\seealso{
\code{\link[=itemsets]{itemsets()}}, \code{\link[=transactions]{transactions()}}
}
